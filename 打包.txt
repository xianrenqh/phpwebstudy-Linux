
# 方式 1：使用构建脚本（最详细，有进度提示）
cd /mnt/works/www_vue/016phpwebstudy
bash build-amd64-deb.sh

# 方式 2：使用 yarn 命令（最简洁）
cd /mnt/works/www_vue/016phpwebstudy
yarn run build:amd64:deb

# 方式 3：手动执行完整命令
cd /mnt/works/www_vue/016phpwebstudy
rm -rf release
yarn run clean
cat scripts/args.esbuild.app-builder | xargs npx esbuild
export USE_SYSTEM_FPM=true NODE_ENV=production ARCH=amd64-deb
node electron/app-builder.js
